# 本仓库这套部署的前端构建期配置。fork 之后，这是前端侧唯一需要改的文件。
#
# 为什么这些值在这里而不是源码里：以前它们是 src/lib/liveEnrollments.ts 里的兜底
# 常量，指向本仓库作者的 VPS。fork 出去的站点在没人察觉的情况下一直在请求上游后端，
# 既拿到别人学校的数据，也在替上游产生流量。现在源码不再有兜底，值只在这里出现一次。
#
# 优先级：Cloudflare Pages 控制台里的同名环境变量 > 本文件（Vite 规则：进程环境
# 里已存在的变量不会被 .env 覆盖）。所以想临时改而不改仓库，去 Pages 控制台加同名变量即可。
#
# 这两个地址由 Go 管理面板的「部署配置」页按你填的「后端对外地址」直接生成，照抄即可。
# 留空 = 关闭实时人数与后端运行配置读取，站点其余功能照常（静态课程数据仍可用）。

VITE_KKAP_API_URL=https://getxk.jxnu-publish.asia/api/enrollments
VITE_BACKEND_CONFIG_URL=https://getxk.jxnu-publish.asia/api/config
