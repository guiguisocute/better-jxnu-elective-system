# 自建数据库替掉 D1 · 方案

状态：**待评审，未实施**。写于 2026-08-31，后端刚迁到校园网机器之后。

## 现状

三个 D1 库，Cloudflare Pages Functions 通过 binding 直连：

| 库 | 大小 | 用途 | 访问方 |
| --- | --- | --- | --- |
| `jxnu-reviews` | ~180 KB | 评价、举报、回收站、操作日志 | Functions + Go 面板 |
| `jxnu-students` | ~367 MB | 学号课表快照（28818 行 blob） | Functions（查不到时回落） |
| `jxnu-ratings` | 小 | 旧星级评分库，保留回滚 | Functions |

关键事实：**`student-record` 这条链路已经是"先打自建后端、失败才回落 D1"的形态**
（`functions/api/student-record.ts` 先请求校园机的 `/live/`，超时才查 D1）。
也就是说最大的那个库已经不是主路径，只是兜底快照。

## 动机与反动机

支持自建：

- 省掉 D1 付费（读多写少但 367MB 的库按行读计费不划算）。
- D1 的慢查询痛点是真实的（见 `reference_d1_page_fetch_cost` 的实测：远端取页
  才是主因，冷库首查 20–25s，现在靠 `d1warm.go` 定时保温硬顶着）。
- 校园机 8C/16G/91G 空闲，已经在跑 Postgres（easygpa 那套），环境是现成的。

反对自建（必须正视）：

- **可用性会变差**。D1 是 Cloudflare 托管的，校园机是一台实验室机器，
  会断电、会被人重启、校园网会维护。评价和举报现在挂了就是全站功能缺失。
- **多一跳跨境**。Functions 跑在 CF 边缘，回源到校园机要经公网 + 覆盖网，
  评价列表这种交互式请求会从个位数毫秒变成几百毫秒。
- 备份、迁移、schema 演进全部变成自己的活。

**结论倾向：分库处理，不要一刀切。**

## 建议路线（按收益/风险排序）

### 第一步：只搬 `jxnu-students`（推荐先做这个）

理由：它占了 367MB 里的全部体量，且**已经有现成的回落语义**——
主路径本来就是实时查教务，D1 只是快照。把快照挪到校园机的本地库，
风险最低、省得最多。

做法：

1. 校园机上建 SQLite（不必上 Postgres：单表 blob、读多写少、28818 行，
   SQLite 足够，且备份就是拷一个文件）。表结构照搬 `d1_schema.sql` 的
   `student_records`。
2. Go 后端加 `GET /student-record/snapshot?sid=`（内网/覆盖网可达，
   复用现有 `X-Live-Secret` 鉴权）。
3. `functions/api/student-record.ts` 的回落分支从「查 D1」改成
   「打后端的 snapshot 接口」。**保留 D1 分支作为第三级兜底**，
   校园机挂了仍能出快照。
4. 观察两周，确认 snapshot 路径命中率与延迟后，再决定是否删 D1 那份。

停机风险：几乎为零（三级回落）。回滚：改回 Functions 里的一个分支。

### 第二步：`jxnu-reviews`（可选，收益小）

180 KB 的库，**留在 D1 基本不花钱**。搬它的唯一理由是"统一"。
但它是交互式读写（用户提交评价要立刻看到），跨境回源到校园机会明显变慢，
而且校园机一挂评价就全挂。

**我的建议是不搬。** 如果一定要搬，必须先做：

- Go 后端加一套评价/举报的 HTTP API（面板已有大部分逻辑，改成对外接口）。
- Functions 全部改成调它，并保留只读降级（校园机不可达时至少能看不能写）。
- 数据库选 Postgres（评价有关联查询、审核状态流转，SQLite 也行但 Postgres
  和现有 easygpa 那套共用运维习惯）。

### 第三步：`jxnu-ratings`

旧库，确认无人读之后直接删，不需要迁移。

## 不建议的做法

- **把 Functions 全部废掉、前端直连校园机**：CORS、鉴权、限流、
  DDoS 全要自己扛，且校园机 IP 一旦被扫就麻烦。CF 边缘这层是有价值的。
- **把数据库直接暴露到公网**：无论 Postgres 还是 SQLite over HTTP，
  都必须只在覆盖网内监听，由 Go 后端做唯一入口（和现在 Cap 的部署形态一致）。

## 需要先确认的问题

1. 校园机的可用性预期是什么？如果接受"偶尔几小时不可用"，第一步就够了；
   如果要求评价功能 7×24，第二步不该做。
2. 备份放哪？校园机本地 + 推到哪台公网机？现在 D1 的备份责任在 Cloudflare，
   自建之后这条要补上，否则 28818 行快照丢了要重爬全校。
3. `jxnu-ratings` 现在还有没有读者？没有的话第三步可以立刻做掉。
